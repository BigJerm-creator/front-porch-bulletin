openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification for The Front Porch Bulletin
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: articles
    description: Newsletter articles
  - name: categories
    description: Article categories
  - name: admin
    description: Admin-only operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /articles:
    get:
      operationId: listArticles
      tags: [articles]
      summary: List all articles
      parameters:
        - name: category
          in: query
          schema:
            type: string
          required: false
        - name: featured
          in: query
          schema:
            type: boolean
          required: false
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
          required: false
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
          required: false
      responses:
        "200":
          description: List of articles
          content:
            application/json:
              schema:
                type: object
                properties:
                  articles:
                    type: array
                    items:
                      $ref: "#/components/schemas/Article"
                  total:
                    type: integer
                required:
                  - articles
                  - total
    post:
      operationId: createArticle
      tags: [articles]
      summary: Create a new article (requires auth)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateArticleBody"
      responses:
        "201":
          description: Article created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Article"
        "401":
          description: Unauthorized

  /articles/featured:
    get:
      operationId: getFeaturedArticles
      tags: [articles]
      summary: Get featured/headline articles
      responses:
        "200":
          description: Featured articles
          content:
            application/json:
              schema:
                type: object
                properties:
                  headline:
                    $ref: "#/components/schemas/Article"
                  secondary:
                    type: array
                    items:
                      $ref: "#/components/schemas/Article"
                required:
                  - secondary

  /articles/summary:
    get:
      operationId: getArticlesSummary
      tags: [articles]
      summary: Get summary stats for all articles
      responses:
        "200":
          description: Summary of article counts by category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArticlesSummary"

  /articles/{id}:
    get:
      operationId: getArticle
      tags: [articles]
      summary: Get a single article
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Article details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Article"
        "404":
          description: Not found
    put:
      operationId: updateArticle
      tags: [articles]
      summary: Update an article (requires auth)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateArticleBody"
      responses:
        "200":
          description: Updated article
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Article"
        "401":
          description: Unauthorized
    delete:
      operationId: deleteArticle
      tags: [articles]
      summary: Delete an article (requires auth)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized

  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List all categories
      responses:
        "200":
          description: List of categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  categories:
                    type: array
                    items:
                      $ref: "#/components/schemas/Category"
                required:
                  - categories

  /admin/users:
    get:
      operationId: listAdminUsers
      tags: [admin]
      summary: List all users with roles (admin only)
      responses:
        "200":
          description: List of users with roles
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      $ref: "#/components/schemas/AdminUser"
                required:
                  - users
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/users/{clerkUserId}/role:
    put:
      operationId: setUserRole
      tags: [admin]
      summary: Grant or update a user's role (admin only)
      parameters:
        - name: clerkUserId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetUserRoleBody"
      responses:
        "200":
          description: Role updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUser"
        "401":
          description: Unauthorized
        "403":
          description: Forbidden
    delete:
      operationId: revokeUserRole
      tags: [admin]
      summary: Revoke a user's role (admin only)
      parameters:
        - name: clerkUserId
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Role revoked
        "401":
          description: Unauthorized
        "403":
          description: Forbidden

  /admin/me:
    get:
      operationId: getMyRole
      tags: [admin]
      summary: Get the current user's role
      responses:
        "200":
          description: Current user's role info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyRoleResponse"
        "401":
          description: Unauthorized

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Article:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        subtitle:
          type: string
          nullable: true
        content:
          type: string
        author:
          type: string
        category:
          type: string
        featured:
          type: boolean
        publishedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - title
        - content
        - author
        - category
        - featured
        - publishedAt
        - createdAt
        - updatedAt

    CreateArticleBody:
      type: object
      properties:
        title:
          type: string
        subtitle:
          type: string
          nullable: true
        content:
          type: string
        author:
          type: string
        category:
          type: string
        featured:
          type: boolean
          default: false
        publishedAt:
          type: string
          format: date-time
      required:
        - title
        - content
        - author
        - category

    UpdateArticleBody:
      type: object
      properties:
        title:
          type: string
        subtitle:
          type: string
          nullable: true
        content:
          type: string
        author:
          type: string
        category:
          type: string
        featured:
          type: boolean
        publishedAt:
          type: string
          format: date-time

    ArticlesSummary:
      type: object
      properties:
        totalArticles:
          type: integer
        totalCategories:
          type: integer
        byCategory:
          type: array
          items:
            type: object
            properties:
              category:
                type: string
              count:
                type: integer
            required:
              - category
              - count
        recentCount:
          type: integer
      required:
        - totalArticles
        - totalCategories
        - byCategory
        - recentCount

    Category:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
        articleCount:
          type: integer
      required:
        - id
        - name
        - slug
        - articleCount

    AdminUser:
      type: object
      properties:
        clerkUserId:
          type: string
        role:
          type: string
        grantedAt:
          type: string
          format: date-time
        email:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
      required:
        - clerkUserId
        - role
        - grantedAt

    SetUserRoleBody:
      type: object
      properties:
        role:
          type: string
          enum: [admin, approved_user]
      required:
        - role

    MyRoleResponse:
      type: object
      properties:
        clerkUserId:
          type: string
        role:
          type: string
          nullable: true
        isAdmin:
          type: boolean
        isApproved:
          type: boolean
      required:
        - clerkUserId
        - isAdmin
        - isApproved
